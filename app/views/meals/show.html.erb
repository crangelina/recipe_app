<div id="show-page">

  <div class="row" id="description">
    <div class="col-sm-12">
      <h1><%= @meal.name %></h1>
    </div>
    <div class="col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
      <p><%= @meal.description %></p>
    </div>
    <div class="col-sm-12">
      <div><%= @meal.tag.capitalize %> | <%= @meal.servings %> servings</div>
      <div>
        <button class="btn btn-link"><%= link_to raw("<i class='fa fa-pencil'></i>"), edit_meal_path(@meal) %></button>
        <button class="btn btn-link"><%= link_to raw("<i class='fa fa-trash'></i>"), meal_path, method: "delete" %></button>
      </div>
    </div>
  </div>

    <div class="row">
      <div class="col-sm-4 col-sm-offset-2 col-xs-12">
        <%= image_tag(@meal.avatar.url, class: "meal-picture-show-page") if @meal.avatar? %>
        <ul id="ingredients">
          <%= render partial: "ingredients/ingredient", collection: @ingredients %>
        </ul>
      </div>

      <div class="col-sm-4 col-xs-10 col-xs-offset-1">
        <ol id="directions">
          <%= render partial: "directions/direction", collection: @directions %>
        </ol>
      </div>
    </div>

</div>
